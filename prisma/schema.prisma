// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  // output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// id          Int       @id @default(autoincrement())

model Rooms {
  id                  String    @id @default(uuid()) @db.VarChar(255)
  name                String?   @db.VarChar(255)
  type                String    @db.VarChar(255)
  description         String?
  dm_key              String?   @db.VarChar(255)     @unique
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt
  rooms_participants  RoomParticipants[]
  chats               Chats[]
  // @@check(role IN ('Admin', 'Intern'))
}

model RoomParticipants {
  id              String    @id @default(uuid()) @db.VarChar(255)
  user_id         String    @db.VarChar(255)
  room_id         String    @db.VarChar(255)
  room            Rooms     @relation(fields: [room_id], references: [id], onDelete: Cascade, onUpdate: Cascade)
  role            String    @db.VarChar(255)
  description     String?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  chats           Chats[]
}

model Chats {
  id              String               @id @default(uuid()) @db.VarChar(255)
  sender          String               @db.VarChar(255)
  RoomParticipant RoomParticipants     @relation(fields: [sender], references: [id], onDelete: Cascade, onUpdate: Cascade)
  chat            String               @db.VarChar(255)
  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt
  room_id         String               @db.VarChar(255)
  room            Rooms                @relation(fields: [room_id], references: [id], onDelete: Cascade, onUpdate: Cascade)
}